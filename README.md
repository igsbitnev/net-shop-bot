# Telegram bot for a restaurant chain — Aiogram 3 example

## О проекте
Минимальный, но полнофункциональный пример бота для сети ресторанов на `aiogram` v3.
Показаны: меню по категориям, корзина, простая система бронирования столиков, хранение пользователей и заказов в SQLite, админ-команды.

## Содержание
- `bot.py` — основной код бота (polling). Меню, заказ, корзина, бронирование, администрация.
- `requirements.txt` — зависимости.
- `config.example.env` — пример переменных окружения.
- `schema.sql` — SQL схема для инициализации БД (опционально).
- `run.sh` — вспомогательный скрипт запуска.
- `db.sqlite` — создаётся автоматически при запуске.

## Быстрый старт
1. Python 3.10+ и виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate
   ```
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Создайте `.env` из примера и впишите `BOT_TOKEN` и `ADMIN_ID`:
   ```bash
   cp config.example.env .env
   # отредактируйте .env
   ```
4. Запустите бота:
   ```bash
   python bot.py
   ```
5. Бот создаст `db.sqlite` и таблицы автоматически при первом запуске.

## Основные команды
- `/start` — запуск и главное меню
- Кнопка `🍽 Меню` — выбрать блюда по категориям, добавить в корзину
- Кнопка `🪑 Забронировать` — простая форма бронирования (дата/время и количество человек)
- `/admin_orders` — (только admin) список последних заказов и бронирований

## Планы для продакшн
- Webhooks, контейнеризация (Dockerfile), миграции, интеграция с POS/CRM, оплата.

## License
MIT
